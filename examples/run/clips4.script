stdlib = import("stdlib")

stdlib.To(stdlib.CLIPS, '(assert (now (Now)))')
stdlib.To(stdlib.CLIPS, '(facts)')
stdlib.To(stdlib.CLIPS, '(clear)')
stdlib.To(stdlib.CLIPS, '(reset)')
stdlib.To(stdlib.CLIPS, '(assert (now (Now)))')
stdlib.To(stdlib.CLIPS, '(facts)')
